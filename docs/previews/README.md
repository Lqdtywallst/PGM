# Previews

This folder keeps non-public HTML previews and working references that still help content or layout decisions.

Rules:

- nothing in this folder is deployed
- previews may read shared assets from `../../site/`
- if a preview stops being useful, delete it instead of moving it back into `site/`
- if a preview becomes production, rebuild or promote the production file inside `site/` instead of serving the preview directly
